# Icons Directory

Place any custom icon files here (SVG preferred).

The portfolio currently uses inline SVGs for all icons, so this directory is optional.

If you want to use external icon files (e.g., for favicons or custom graphics), place them here and reference them in `index.html`.
