# Resume Directory

Place your resume/CV here as `resume.pdf`.

The hero section download button links to:
```
assets/resume/resume.pdf
```

## Tips

- Keep the filename as `resume.pdf` so the download link works without changes
- If you rename the file, update the `href` in `index.html` (hero section, line ~118)
- PDF format is recommended for universal compatibility
